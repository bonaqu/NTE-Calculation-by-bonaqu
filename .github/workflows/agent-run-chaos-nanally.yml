name: Run guarded Chaos Nanally feature patch

on:
  pull_request:
    branches:
      - bonaqu_projects
    types: [opened, synchronize, reopened]

permissions:
  contents: write

jobs:
  patch-feature-branch:
    if: github.head_ref == 'feat/chaos-ultimate-actions'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
        with:
          ref: feat/chaos-ultimate-actions
          fetch-depth: 0
      - name: Check whether patch is pending
        id: guard
        shell: bash
        run: |
          if [[ -f tools/apply-chaos-nanally-patch.py && ! -f src/verified-visible-actions-batch-i.ts ]]; then
            echo "apply=true" >> "$GITHUB_OUTPUT"
          else
            echo "apply=false" >> "$GITHUB_OUTPUT"
          fi
      - uses: actions/setup-node@v6
        if: steps.guard.outputs.apply == 'true'
        with:
          node-version: 24
      - name: Apply guarded patch
        if: steps.guard.outputs.apply == 'true'
        run: |
          python tools/apply-chaos-nanally-patch.py
          python tools/fix-chaos-nanally-generated-test.py
      - name: Install dependencies
        if: steps.guard.outputs.apply == 'true'
        run: npm install --no-audit --no-fund
      - name: Typecheck
        if: steps.guard.outputs.apply == 'true'
        run: npm run typecheck
      - name: Unit tests
        if: steps.guard.outputs.apply == 'true'
        run: npm test
      - name: Production build
        if: steps.guard.outputs.apply == 'true'
        run: npm run build
      - name: Worker dry run
        if: steps.guard.outputs.apply == 'true'
        run: npm run worker:check
      - name: Publish verified feature commit
        if: steps.guard.outputs.apply == 'true'
        run: |
          rm tools/apply-chaos-nanally-patch.py
          rm tools/fix-chaos-nanally-generated-test.py
          rm .github/workflows/agent-chaos-nanally-patch.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat: complete Nanally and model Chaos burst actions"
          git push origin HEAD:feat/chaos-ultimate-actions
